// Service Worker — Conciliador Pro// Permite notificaciones en background (PWA)self.addEventListener("install", () => self.skipWaiting());self.addEventListener("activate", (e) => e.waitUntil(self.clients.claim()));// Escucha mensajes del cliente para mostrar notificacionesself.addEventListener("message", (event) => {  if (event.data?.type === "NOTIFY") {    const { title, body, tag, icon } = event.data;    self.registration.showNotification(title, {      body,      tag: tag || "conciliador",      icon: icon || "/favicon.ico",      badge: "/favicon.ico",      requireInteraction: true,      vibrate: [200, 100, 200],    });  }});// Click en notificación → enfocar la pestañaself.addEventListener("notificationclick", (event) => {  event.notification.close();  event.waitUntil(    self.clients.matchAll({ type: "window" }).then((clients) => {      for (const client of clients) {        if ("focus" in client) return client.focus();      }      if (self.clients.openWindow) return self.clients.openWindow("/");    })  );});